document.addEventListener("DOMContentLoaded", function() {
    // JavaScript for horizontal orientation
    function scrollJson(direction) {
        // Logic to scroll JSON content up or down
    }

    function changeDesign() {
        // Logic to switch styles
    }

    window.scrollJson = scrollJson;
    window.changeDesign = changeDesign;
});